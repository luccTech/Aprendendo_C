{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build",
        "type": "shell",
        "command": "gcc",
        "args": [
          "${file}",
          "-o",
          "${fileDirname}\\${fileBasenameNoExtension}.exe"
          // Remova a flag -Wunknown-pragmas
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }
  